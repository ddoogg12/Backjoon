# 주유소

n = int(input()) # 도시수 입력

d = list(map(int, input().split()))
m = list(map(int, input().split()))

result = 0
distance = 0

while len(m) > 1:
	if m[i] > m[i+1]:
		result += m[i] * d[i]
	else:
		m.pop([i+1])
		distance += ( d[i] + d[i+1])
	
print(d)
print(m)
print(result)

