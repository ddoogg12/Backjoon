#실버5
# 단어정렬(1181)

n = int(input())
array = []
for i in range(n):
	array.append(input())

array = list(set(array))  #중복 제거

array.sort()  #사전순으로 정렬
array.sort(key=lambda x:len(x)) #길이순으로 정렬
for i in array:
	print(i)
